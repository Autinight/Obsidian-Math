# 原子化笔记软件构想

## 初始动机

想通过 vibe coding 做一个可以长期推进的练手项目。项目一开始要足够简单，每天都能推进一点；但底层形态要可扩展，之后可以逐渐发展成一个很大的系统。

比起凭空从零设计，更倾向于先复刻某个开源项目的基座，再在其上做个人定制。但这里的“复刻”更应该理解为复现核心结构、交互和数据流，而不是直接 fork 一个复杂项目然后被原项目的历史包袱拖住。

## 核心想法

想做一个真正的原子化笔记软件。

现有笔记主要放在 Obsidian 仓库里，以卡片形式管理每个定理、定义、命题等，再通过 atlas 把它们拼接起来，形成定理和知识单元的复用。

当前痛点在于：卡片本身已经比较原子化，但 atlas 是显式维护的，因此天然存在不完备问题。很多关系需要人工补全、人工记忆、人工组织，使用体验会变得麻烦。

希望构建一个更适合这种原子化笔记哲学的产品。

## 对 Obsidian 的判断

不希望过度依赖 Obsidian。

Obsidian 的双链关系本质上不够牢固。双链通常只能表达：

```text
A 提到了 B
```

但真正需要的是更强、更明确、更可计算的关系。例如：

```text
A 依赖 B
A 使用 B
A 推出 B
A 是 B 的特例
A 是 B 的推广
A 与 B 等价
A 反驳 B
A 是 B 的例子
```

因此，Obsidian 可以作为已有资产、导入来源或导出目标，但不应该成为底层依赖。系统的事实源应该是自己的数据结构，而不是 markdown 文件之间的双链。

## 从数学场景出发，但不局限于数学

一开始的直觉来自数学笔记：definition、theorem、lemma、proposition、proof 等都可以作为原子对象。

但进一步看，这个项目不应该被数学类型锁死。它应该是一类笔记哲学的构建。

也就是说：

```text
definition 不是系统内建死的类型。
definition 只是用户自定义出来的一种 AtomType。
```

同理，写作里的 idea、scene、quote，研究里的 claim、evidence、question、source，也都应该能用同一套机制表达。

## Atom 的初步定义

Atom 是一个可以被独立命名、引用、复用、连接、重组的最小认知单位。

它不是普通页面，也不是普通 markdown block。它应该是系统中最基础的知识对象。

一个东西是否应该成为 Atom，可以用这些问题判断：

```text
它能不能被独立引用？
它能不能被复用？
它能不能和其他对象建立明确关系？
它能不能在不同视图或 atlas 中被重新组织？
```

如果可以，它就有资格成为 Atom。

## AtomType

Atom 本身不应该被写死为某个具体学科对象。真正解释 Atom 是什么的，是 AtomType。

AtomType 是用户可定义的类型系统。

例如数学场景中可以有：

```text
Definition
Theorem
Lemma
Proposition
Corollary
Example
Counterexample
Proof
Technique
Remark
```

写作场景中可以有：

```text
Idea
Scene
Character
Quote
Fragment
Theme
```

研究场景中可以有：

```text
Claim
Evidence
Question
Source
Method
Observation
```

这些都不应该是系统底层的硬编码类型，而应该是用户定义出的 AtomType。

## 字段系统

每种 AtomType 应该可以拥有自己的字段。

例如数学中的 Definition 可以有：

```text
Statement
Intuition
Examples
Related Concepts
```

Theorem 可以有：

```text
Statement
Assumptions
Proof Sketch
Consequences
```

写作中的 Scene 可以有：

```text
Summary
Characters
Draft
Purpose
```

因此，第一版就应该考虑：

```text
Atom = 通用对象
AtomType = 用户定义的类型
Field = 类型上的结构化字段
```

## 关系系统的方向

当前阶段先不急着完整实现关系系统，但未来关系应该是一等对象。

关系不应该只是链接，而应该具有类型、方向和语义。

可能的 RelationType：

```text
depends_on
uses
implies
equivalent_to
generalizes
specializes
example_of
counterexample_to
motivates
analogous_to
dual_to
```

对于非数学场景，也可以自定义：

```text
supports
contradicts
belongs_to
continues
inspired_by
summarizes
expands
```

长期目标是：

```text
Atom 不关心自己是什么。
AtomType 解释它是什么。
RelationType 解释它和别的 Atom 发生了什么关系。
View 解释用户如何观看这些 Atom。
```

## Atlas 的重新定义

Atlas 不应该是手写目录。

Atlas 应该是基于 Atom 与 Relation 自动生成或半自动生成的视图。

也就是说：

```text
手写 atlas -> 显式维护，容易不完备
生成 atlas -> 从关系图中投影出来，更稳定
```

一个 atlas 可以是某个主题下的学习路径、知识地图、证明依赖链、写作结构、项目结构，或者其他自定义视图。

## 最小可行版本

第一阶段先不要做完整笔记软件，也不要做复杂图谱。

最小功能可以只围绕 Atom 设计：

```text
1. 创建 AtomType
2. 为 AtomType 定义字段
3. 创建 Atom
4. 根据 AtomType 渲染不同表单
5. 查看 Atom 列表
6. 点开并编辑 Atom
```

这已经可以验证产品内核：用户不是在写普通页面，而是在定义自己的知识对象系统。

## 初步数据模型草案

```ts
type Atom = {
  id: string;
  typeId: string;
  title: string;
  content: string;
  properties: Record<string, unknown>;
  status: "draft" | "active" | "archived";
  createdAt: string;
  updatedAt: string;
};

type AtomType = {
  id: string;
  name: string;
  description?: string;
  fields: AtomField[];
  template?: string;
  createdAt: string;
  updatedAt: string;
};

type AtomField = {
  key: string;
  label: string;
  kind:
    | "text"
    | "markdown"
    | "number"
    | "date"
    | "select"
    | "multiSelect"
    | "boolean"
    | "atomRef"
    | "atomRefs";
  required?: boolean;
  options?: string[];
};
```

后续可以加入：

```text
Relation
RelationType
View
Collection
Atlas
Template
ImportAdapter
ExportAdapter
```

## 产品定位草案

一个允许用户构造个人知识本体的原子笔记系统。

更短一点：

```text
A typed atomic note system.
```

或者：

```text
一个基于 Atom、Type、Relation、View 的个人知识构造工具。
```

## 暂定原则

```text
Atom 是最小知识单位。
AtomType 是分类哲学。
Field 是表达结构。
Relation 是语义连接。
Atlas / View 是结构的投影。
Obsidian 只是导入导出适配器，不是事实源。
```

## 下一步可讨论的问题

```text
1. Atom 的最小字段到底有哪些？
2. AtomType 第一版要做到多自由？
3. Field 系统是否需要从第一天就支持 atomRef？
4. Relation 是否要进入 MVP，还是第二阶段再做？
5. 复刻哪个开源项目的基座最合适？
6. 第一版是 Web app、本地 app，还是先 Web 后 Tauri？
7. 是否要预设一套数学 AtomType 模板？
```
