0. 通过MinerU api或本地minerU, 转换成json, 由于minerU是把文档解析成
    1. 这里涉及到的一个问题是, 由于页码上线是200页, 而书目的页数通常超过这个, 我们需要得到多个json文件后, 合并成一个json, 并修正index的数值. 
1. 让llm获得书目的目录架构信息, 这部分从书目的目录本身能直接得到, 获得一个目录的带有层级的表. 用类似多元数组的数据来存储标题信息. 数组的长度代表了所属的层级, 比如chapter1可以用(1)储存, section1.2可以用(1,2)储存.
2. 
